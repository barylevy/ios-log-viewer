{
  "name": "ios-log-viewer",
  "version": "1.0.0",
  "description": "iOS Log Viewer with AI Chat",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build"
  }
}
